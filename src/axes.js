import React from "react";

export { XAxis, YAxis };

function XAxis (props) {
    
    var chartType = 'bar'; //remove this line when you write your own code

    if (chartType === "scatter") {
        return <g>
            
        </g>
    }
    if (chartType === "bar") {
        return <g>
        </g>
    }
}

function YAxis(props) {
   
    return <g>
        
    </g>
    
}